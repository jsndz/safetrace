apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: auth-pgdata
  name: auth-pgdata
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
